apiVersion: apps/v1
kind: Deployment
metadata:
  name: mongodb
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: mongodb
  template:
    spec:
      containers:
        - name: mongodb
          imagePullPolicy: IfNotPresent